# A Neural network written from scratch
## A basic neural network written from scratch that predicts numbers from MNIST hand written images.

## Prerequisites
### Install all dependencies using the following command
```bash
    pip install -r requirements.txt
```
## 📦 Installation & Usage
### 1. Clone the repository
```bash
    git clone git@github.com:jenovax86/Neural-Network.git
    cd Neural-Network
```
### 2. Train the neural network
```bash
    python -m model
```
### 3. Generate predictions
```bash
    python -m example
```
